{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local", "bun.lock"],
  "globalEnv": ["USEPRINT_DISABLE_REMOTE_FONTS"],

  "tasks": {
    "build": {
      "inputs": ["packages/**"],
      "dependsOn": ["^build", "^postbuild"],
      "outputs": ["dist/**"]
    },
    "postbuild": {
      "dependsOn": ["build"],
      "cache": false
    },
    "web#build": {
      "dependsOn": ["^build", "^postbuild"],
      "env": [
        "useprint_API_KEY",
        "NEXT_PUBLIC_SUPABASE_URL",
        "NEXT_PUBLIC_SUPABASE_ANON_KEY"
      ],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "@useprint/preview#build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "demo#build": {
      "dependsOn": ["^build", "^postbuild"],
      "outputs": [".useprint/**", "!.useprint/.next/cache/**"]
    },
    "useprint#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {},
    "//#format": {},
    "//#format:check": {},
    "test": {
      "dependsOn": ["^build"]
    },
    "test:watch": {
      "cache": false
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    }
  }
}
